<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lastoneforall">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/label"
        android:label="মুক্তবাক"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".texttosignvideo"></activity>
        <activity android:name=".Keyboardforbothir" />
        <activity android:name=".videoplayer" />
        <activity android:name=".showmyresult" />
        <activity android:name=".play_video" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>